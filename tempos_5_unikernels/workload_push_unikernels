cd /opt/unikernels_workload/apache-activemq;echo "bla2" >> README.md;git commit -am "vai";git push

cd /opt/unikernels_workload/apache-zookeeper;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/apache-spark;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/cassandra;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/haproxy;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/memcached;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/redis-memonly;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/solr;echo "bla2" >> README;git commit -am "vai";git push

cd /opt/unikernels_workload/tomcat;echo "bla2" >> README;git commit -am "vai";git push

