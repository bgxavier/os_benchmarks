---
  HeatStacks.create_and_delete_stack:
    -
      args:
        template_path: "heat_libvirt_unikernel.yaml"
      runner:
        type: "constant"
        times: 10
        concurrency: 1
      context:
        users:
          tenants: 1
          users_per_tenant: 1

